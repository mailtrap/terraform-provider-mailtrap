---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "mailtrap_webhook Resource - mailtrap"
subcategory: ""
description: |-
  Manages a Mailtrap webhook that delivers event notifications to a URL.
---

# mailtrap_webhook (Resource)

Manages a Mailtrap webhook that delivers event notifications to a URL.

## Example Usage

```terraform
resource "mailtrap_webhook" "example" {
  url          = "https://example.com/mailtrap/events"
  webhook_type = "email_sending"
  event_types  = ["delivery", "bounce", "spam_complaint"]
}

# Secret for verifying payload signatures. Only available when the webhook
# is created by Terraform (never returned again by the API).
output "signing_secret" {
  value     = mailtrap_webhook.example.signing_secret
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `url` (String) URL event payloads are delivered to.
- `webhook_type` (String) Kind of webhook. Changing this forces replacement.

### Optional

- `active` (Boolean) Whether the webhook is active. Defaults to `true`.
- `domain_id` (Number) Sending domain the webhook is scoped to. Changing this forces replacement.
- `event_types` (List of String) Event types the webhook subscribes to (`email_sending` and `campaigns` webhooks).
- `inbound_inbox_id` (Number) Inbound inbox the webhook is scoped to. Changing this forces replacement.
- `payload_format` (String) Encoding of delivered payloads.
- `sending_stream` (String) Sending stream the webhook applies to (`email_sending` webhooks only). Changing this forces replacement.

### Read-Only

- `id` (Number) Numeric identifier of the webhook.
- `signing_secret` (String, Sensitive) HMAC SHA-256 secret for verifying payload signatures. Returned only on creation; unavailable for imported webhooks.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import a webhook by its numeric ID. The signing_secret cannot be recovered
# on import; it is only returned by the API when the webhook is created.
terraform import mailtrap_webhook.example 12345
```
